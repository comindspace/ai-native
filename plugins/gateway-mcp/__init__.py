"""Hermes plugin placeholder for remote GatewayMCP integration.

Configure the remote MCP endpoint with GATEWAY_MCP_URL and
GATEWAY_MCP_TOKEN in the host agent/plugin settings.
"""


def register(ctx):
    del ctx
